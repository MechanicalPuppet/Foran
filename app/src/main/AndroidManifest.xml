<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hernandez.brandon.foran">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Foran">
        <activity android:name=".DeleteProfileActivity"></activity>
        <activity android:name=".ForumPosts" />
        <activity android:name=".ForumActivity" />
        <activity android:name=".RecipeDetail" />
        <activity android:name=".DetailRecipe" />
        <activity android:name=".RecipesActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".TransportActivity"
            android:label="@string/title_activity_transport"
            android:theme="@style/Theme.Foran.NoActionBar" />
        <activity android:name=".UbicationActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".RestorePasswordNewPasswordActivity" />
        <activity android:name=".RestorePasswordCodeActivity" />
        <activity android:name=".RestorePasswordActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>